import request from '../utils/request'
import qs from 'qs'
import base from './config'

function login (params) {
  return request.post(`${base.url}/login`, qs.stringify(params))
}

export default login()
